% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{loo_contribution_analysis}
\alias{loo_contribution_analysis}
\title{Leave-one-out contribution analysis}
\usage{
loo_contribution_analysis(networks, metaweb)
}
\arguments{
\item{networks}{A named list of matrices, ordered by time point.}

\item{metaweb}{The pooled metaweb, e.g. from \code{\link[=build_metaweb]{build_metaweb()}}.}
}
\value{
A data frame, one row per network, with expected/observed-unique
species and interaction counts (raw and as a \% of the metaweb) and
\code{species_efficiency} / \code{interaction_efficiency}.
}
\description{
For each network, compares its expected contribution to the pooled
metaweb against its observed-unique contribution relative to the
leave-one-out metaweb (i.e. what it adds that no other period covers).
An efficiency index (observed unique / expected) summarizes how much of
each network's apparent contribution is actually unique to it.
}
