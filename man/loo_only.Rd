% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{loo_only}
\alias{loo_only}
\title{Leave-one-out contribution analysis without running the full pipeline}
\usage{
loo_only(x, metaweb = NULL)
}
\arguments{
\item{x}{A named list of matrices, or a \code{bd_pipeline_config} (networks are
loaded via \code{\link[=load_networks]{load_networks()}}).}

\item{metaweb}{Optional pooled metaweb; built automatically with
\code{\link[=build_metaweb]{build_metaweb()}} when NULL. If \code{x} is a config whose \code{cfg$metaweb} file
exists, that file is used instead unless \code{metaweb} is supplied explicitly.}
}
\value{
A data frame, see \code{\link[=loo_contribution_analysis]{loo_contribution_analysis()}}.
}
\description{
Skips null models, rarefaction, beta diversity and per-network metrics --
only builds the metaweb (and leave-one-out metawebs) and runs
\code{\link[=loo_contribution_analysis]{loo_contribution_analysis()}}.
}
